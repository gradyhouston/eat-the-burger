<tr class="table-row">
    <td class="table-first-col">{{id}}. {{burger_name}}</td>
    <td>
      <!-- <input type="text" id="eat-person-" style="width: 125%" placeholder="Who is this for?"> -->
      <button class="change-devour" data-id="{{id}}">Eat me!</button>
    </td>
</tr>
